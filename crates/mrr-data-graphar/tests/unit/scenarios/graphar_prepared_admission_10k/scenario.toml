id = "graphar-prepared-admission-10k"
title = "Prepared GraphAr semantic facts admit without native storage re-entry"
policy_ids = ["RUST-AGENT-ASP-SCENARIO-BENCH-001"]
agent_goal = "Reject a prepared-source path that re-enters GraphAr storage, drops any of 10,000 MRR facts, or exceeds the fixed semantic-admission budgets."
reference_repositories = ["apache/incubator-graphar", "tao3k/asp-rust"]
reference_patterns = ["PreparedGraphArSource", "Fact", "asp_rust_scenario"]
inputs = "inputs"
expected = "expected"
