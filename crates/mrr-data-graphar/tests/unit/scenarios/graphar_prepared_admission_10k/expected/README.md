# Expected

Every measured iteration must consume the same immutable typed MRR facts,
restore all 10,000 canonical facts through the supplied projection, and perform
no native GraphAr storage read, Arrow C Stream import, persisted identity parse,
or canonical sort.
