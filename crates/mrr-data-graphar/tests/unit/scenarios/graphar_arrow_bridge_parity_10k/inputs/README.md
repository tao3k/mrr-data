# Input

The focused Rust Scenario deterministically writes 10,000 binary-Entity MRR
edges, then applies the same eleven-property projection to the official GraphAr
Arrow chunk reader and the Rust Arrow C Stream bridge.
