id = "graphar-arrow-bridge-parity-10k"
title = "GraphAr storage I/O and zero-copy Arrow C Stream import remain independently bounded"
policy_ids = ["RUST-AGENT-ASP-SCENARIO-BENCH-001"]
agent_goal = "Reject a Rust GraphAr read that regresses more than 25 percent against the official chunk reader, or whose zero-copy Arrow C Stream import exceeds one millisecond for the identical 10,000-edge projection."
reference_repositories = ["apache/incubator-graphar", "tao3k/asp-rust"]
reference_patterns = ["AdjListArrowChunkReader", "AdjListPropertyArrowChunkReader", "ExportRecordBatchReader", "ArrowArrayStreamReader", "Arrow C Stream", "asp_rust_scenario"]
inputs = "inputs"
expected = "expected"
