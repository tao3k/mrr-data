id = "graphar-arrow-bridge-parity-10k"
title = "GraphAr storage I/O stays observable while zero-copy Arrow C Stream import is bounded"
policy_ids = ["RUST-AGENT-ASP-SCENARIO-BENCH-001"]
agent_goal = "Reject a zero-copy Arrow C Stream import that exceeds either one millisecond or ten percent of the official GraphAr read for the identical 10,000-edge projection; retain the independent storage reads as diagnostic phases rather than conflating their I/O variance with ABI overhead."
reference_repositories = ["apache/incubator-graphar", "tao3k/asp-rust"]
reference_patterns = ["AdjListArrowChunkReader", "AdjListPropertyArrowChunkReader", "ExportRecordBatchReader", "ArrowArrayStreamReader", "Arrow C Stream", "asp_rust_scenario"]
inputs = "inputs"
expected = "expected"
