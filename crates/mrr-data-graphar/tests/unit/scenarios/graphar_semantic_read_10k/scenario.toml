id = "graphar-semantic-read-10k"
title = "GraphAr Arrow chunks reconstruct 10,000 canonical MRR facts"
policy_ids = ["RUST-AGENT-ASP-SCENARIO-BENCH-001"]
agent_goal = "Keep native GraphAr chunk reading and typed MRR semantic admission below the fixed performance budgets without reducing the corpus."
reference_repositories = ["apache/incubator-graphar", "tao3k/asp-rust"]
reference_patterns = ["ArrowChunkReader", "asp_rust_scenario", "semantic admission"]
inputs = "inputs"
expected = "expected"
